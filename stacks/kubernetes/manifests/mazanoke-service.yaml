apiVersion: v1
kind: Service
metadata:
  name: mazanoke
  labels:
    app: mazanoke
spec:
  type: NodePort
  selector:
    app: mazanoke
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30474
